# 2026-06-10 日志

## 密码找回
- 成功找回了密码
- 方法：通过 recovery 界面恢复密码

## 机房电脑资源
- 发现机房电脑配置较好
- GPU 显存：12G
- 决定：以后在机房进行相关工作

## Node 5 图像识别链路优化
- 发现问题：原 Node 5 默认只读取 `data/test_samples/warehouse_photo1.png` 静态截图，不会自动读取 Isaac Sim 当前相机画面。因此仿真视角、物体位置或光照变化后，模型仍可能在看旧图，容易误判。
- 当前模型状态：
  - 本地模型：`/home/bluepoisons/Desktop/FURP/VLN/models/Qwen3-VL-2B-Instruct`
  - 模型目录大小约 `4.0G`
  - 当前机器 GPU：RTX 3060 12GB
  - 检查时显存状态：总显存约 12GB，已用约 3.5GB，可用约 8.5GB
  - 结论：当前硬件可以优先尝试 GPU 4-bit 推理，不需要默认强制 CPU；CPU 更适合作为兼容兜底。
- 代码优化：
  - `vln_node_local.py` 新增 `image_topic` 和 `compressed_image_topic` 参数，支持订阅实时 `sensor_msgs/Image` 或 `sensor_msgs/CompressedImage`，并保存为 `data/runtime/latest_camera.png` 供本地 Qwen 推理使用。
  - `run_inference_cli.py` 和 `qwen_model_wrapper.py` 统一改成只解码模型新生成内容，避免把 prompt 也混进输出。
  - prompt 改为要求模型返回紧凑 JSON：`target, visible, confidence, x, y, yaw`，便于后续做 Node 6 定量统计。
  - `text_to_pose_converter.py` 改为优先解析模型 JSON，只有没有 JSON 时才使用关键词坐标兜底。日志中 `method=json` 才代表模型输出真正参与了目标解析。
  - `force_cpu` 改为可配置项，12GB 显存机器上优先尝试 GPU 4-bit 推理，CPU 仅作为兼容兜底。
  - `package.xml` 增加 `sensor_msgs` 依赖。
  - `vln_nav2_bridge_local.launch.py` 增加 `image_topic`、`compressed_image_topic`、`require_fresh_image` 和 `force_cpu` 启动参数。
- 验证：
  - `colcon build --packages-select vln_nav2_bridge` 通过。
  - ROS `Image(rgb8/bgr8)` 到 PIL RGB 的解码小测试通过。
  - `text_to_pose_converter.py` 验证通过：
    - 模型 JSON 输出会解析为 `method=json`
    - 无 JSON 时才退回 `method=instruction_fallback`
  - 用静态样例图跑了一次真实 Qwen 推理，命令如下：
    ```bash
    conda run -n isaaclab python src/vln_inference/run_inference_cli.py \
      --model-path /home/bluepoisons/Desktop/FURP/VLN/models/Qwen3-VL-2B-Instruct \
      --image-path /home/bluepoisons/Desktop/FURP/VLN/ZhiruiSHEN-VLN/data/test_samples/warehouse_photo1.png \
      --instruction 'Go to the plant' \
      --max-new-tokens 128
    ```
  - 模型返回了可解析 JSON：
    ```json
    {
      "target": "plant",
      "visible": true,
      "confidence": 0.95,
      "x": -0.43,
      "y": -2.92,
      "yaw": 0.0
    }
    ```
  - 当前 `ros2 topic list` 只看到 `/parameter_events` 和 `/rosout`，说明本次检查时 Isaac 相机 ROS topic 尚未发布；需要在 Isaac Sim 中配置 ROS2 Camera Helper 后再做实时视觉联调。

### 新版 Node 5 启动方式

基础启动仍然可用，不传相机 topic 时会继续使用静态样例图：

```bash
source /opt/ros/humble/setup.bash
cd /home/bluepoisons/Desktop/FURP/VLN/ZhiruiSHEN-VLN
source install/setup.bash
ros2 run vln_nav2_bridge vln_node_local --ros-args -p use_sim_time:=True
```

接入 Isaac 实时相机后，推荐使用 launch 参数启动：

```bash
source /opt/ros/humble/setup.bash
cd /home/bluepoisons/Desktop/FURP/VLN/ZhiruiSHEN-VLN
source install/setup.bash
ros2 launch vln_nav2_bridge vln_nav2_bridge_local.launch.py \
  image_topic:=/YOUR_CAMERA_IMAGE_TOPIC \
  require_fresh_image:=true \
  force_cpu:=false
```

如果 Isaac 发布的是压缩图像，则使用：

```bash
ros2 launch vln_nav2_bridge vln_nav2_bridge_local.launch.py \
  compressed_image_topic:=/YOUR_COMPRESSED_IMAGE_TOPIC \
  require_fresh_image:=true \
  force_cpu:=false
```

查找 Isaac 相机 topic：

```bash
ros2 topic list | rg 'image|rgb|camera'
```

### 当前判断

- Node 5 之前的“识别差”主要不是 Qwen3-VL-2B 完全不可用，而是视觉输入链路没有接实时相机。
- 现在静态图上模型能按要求输出结构化 JSON，说明后续重点应该放在实时相机、视角质量、目标可见性和评估指标上。
- 后续 Node 6 评估时不要只看导航成功率，还要记录：
  - `visible`
  - `confidence`
  - `method=json` 或 `method=instruction_fallback`
  - 模型目标名称是否正确
  - 最终坐标是否来自模型 JSON 还是规则兜底

### 后续优化方向

1. 在 Isaac Sim 中给机器人加 RGB Camera，并通过 ROS2 Camera Helper 发布实时 RGB 图像。
2. 先用 `require_fresh_image:=true` 强制检查实时图像是否真的进入 Node 5，避免静态图兜底掩盖问题。
3. 为 Node 6 准备 15 组语言指令，按 `plant/chair/purple boxes/shelf/unknown target` 分类统计。
4. 如果实时图像里小目标识别仍差，优先增加开放词表检测前端，例如 GroundingDINO 或 YOLO-World，再把检测框/类别交给 Qwen 做语言到地图目标解析。
5. 2B 模型继续作为在线主模型；后续可以把 8B/更大模型作为离线复核或关键帧二次判读，不建议一开始直接替换在线主链路。

## Node 6 全自动评估脚本

- 已新增 `node6.md`，整理 Node 6 启动流程、任务定义、15 条默认评估指令、记录字段和完成标准。
- 已新增 `vln_nav2_bridge.node6_auto_trials` 自动评估节点，并注册为 ROS 可执行入口：
  ```bash
  ros2 run vln_nav2_bridge node6_auto_trials
  ```
- 自动评估脚本能力：
  - 自动向 `/vln_instruction` 逐条发布 15 条 Node 6 指令。
  - 自动等待 bridge 在 `/vln_trial_result` 返回每条任务结果。
  - 自动记录模型输出目标、可见性、置信度、解析方式、目标坐标、导航结果和失败原因。
  - 自动订阅 `/amcl_pose` 获取最终机器人位姿。
  - 如果 `/amcl_pose` 暂时没有数据，使用 `/chassis/odom` 作为最终位姿兜底，并在 `final_pose_source` 中标记来源。
  - 自动计算 `final_error_m` 和 `within_success_radius`。
- 输出文件：
  ```text
  data/node6_trials.csv       # bridge 每条任务的原始追加日志
  data/node6_auto_trials.csv  # 自动评估汇总表
  ```
- 已补充 bridge 结果发布：
  - `/vln_trial_result`，类型 `std_msgs/msg/String`
  - payload 为 JSON，包含 `target_x, target_y, target_yaw, nav_result, failure_reason` 等字段。
- 已补充依赖：
  - `nav_msgs`
- 验证：
  - `/usr/bin/python3 -m compileall ...` 通过。
  - `colcon build --packages-select vln_nav2_bridge` 通过。
  - `ros2 run vln_nav2_bridge node6_auto_trials --help` 可用，并显示 `--pose-topic`、`--odom-topic`、`--success-radius-m` 参数。
  - 已重启 Node 5 bridge，当前 bridge 发布 `/vln_trial_result`，订阅 `/front_stereo_camera/left/image_raw` 和 `/vln_instruction`。
  - 当前可用关键话题：
    ```text
    /front_stereo_camera/left/image_raw
    /vln_instruction
    /vln_trial_result
    /amcl_pose
    /chassis/odom
    ```

### Node 6 自动评估启动命令

```bash
source /opt/ros/humble/setup.bash
cd /home/bluepoisons/Desktop/FURP/VLN/ZhiruiSHEN-VLN
source install/setup.bash
ros2 run vln_nav2_bridge node6_auto_trials \
  --output /home/bluepoisons/Desktop/FURP/VLN/ZhiruiSHEN-VLN/data/node6_auto_trials.csv \
  --timeout-sec 240 \
  --pose-topic /amcl_pose \
  --odom-topic /chassis/odom \
  --success-radius-m 0.8
```

### 自动评估写 CSV 错误修复

- 现象：
  - Trial 1 已成功：
    ```text
    nav_result=success, parse_method=json, target=(-0.43, -2.92)
    final=(-0.209, -2.885), error_m=0.224
    ```
  - 随后自动评估脚本报错：
    ```text
    IsADirectoryError: [Errno 21] Is a directory: '/home/bluepoisons/Desktop/FURP/VLN/ZhiruiSHEN-VLN/data/'
    node6_auto_trials.csv: command not found
    ```
- 原因：
  - shell 换行导致 `--output` 实际只收到目录 `/home/.../data/`。
  - 下一行 `node6_auto_trials.csv` 被 shell 当成命令执行。
  - 导航和 bridge 结果没有失败，失败只发生在自动汇总 CSV 写入阶段。
- 修复：
  - `node6_auto_trials.py` 新增输出路径容错：如果 `--output` 是目录，自动写入该目录下的 `node6_auto_trials.csv`。
  - 新增 `--start-index` 参数，支持从指定默认 trial 继续运行。
  - 从 Trial 2 继续时，日志显示 `Trial 2/15`，CSV 中 `trial_id` 也保持为 `2`。
- 验证：
  - `/usr/bin/python3 -m compileall src/vln_nav2_bridge/vln_nav2_bridge/node6_auto_trials.py` 通过。
  - `colcon build --packages-select vln_nav2_bridge` 通过。
  - `ros2 run vln_nav2_bridge node6_auto_trials --help` 已显示 `--start-index`。

从 Trial 2 继续运行命令：

```bash
source /opt/ros/humble/setup.bash
cd /home/bluepoisons/Desktop/FURP/VLN/ZhiruiSHEN-VLN
source install/setup.bash
ros2 run vln_nav2_bridge node6_auto_trials \
  --output /home/bluepoisons/Desktop/FURP/VLN/ZhiruiSHEN-VLN/data/node6_auto_trials_from_2.csv \
  --start-index 2 \
  --timeout-sec 240 \
  --pose-topic /amcl_pose \
  --odom-topic /chassis/odom \
  --success-radius-m 0.8
```

### Node 6 重置后全自动重跑完成

- 当前状态：
  - 用户已手动启动 Isaac Sim，并重置小车位置。
  - 除 Isaac Sim 外的 ROS 终端重新启动完成。
- 本次补充启动项：
  - `map -> odom` 静态 TF。
  - `pointcloud_to_laserscan`，实际 remap：
    ```text
    cloud_in:=/front_3d_lidar/lidar_points
    scan:=/scan
    ```
  - Nav2/RViz。
  - `base_link -> base_footprint` 静态 TF，用于解决 AMCL 需要 `base_footprint` 的问题。
  - `/initialpose` 初始化 AMCL。
  - VLN bridge：
    ```bash
    ros2 launch vln_nav2_bridge vln_nav2_bridge_local.launch.py \
      image_topic:=/front_stereo_camera/left/image_raw \
      require_fresh_image:=true \
      force_cpu:=false \
      gpu_device:=0
    ```
- 自动评估命令：
  ```bash
  ros2 run vln_nav2_bridge node6_auto_trials \
    --output /home/bluepoisons/Desktop/FURP/VLN/ZhiruiSHEN-VLN/data/<历史输出文件>.csv \
    --start-index 1 \
    --timeout-sec 900 \
    --pose-topic /amcl_pose \
    --odom-topic /chassis/odom \
    --success-radius-m 0.8
  ```
- 该次历史 CSV 已在数据清理时删除，摘要结果保留在本日志中。
- 运行结果：
  ```text
  total trials: 15
  navigation success: 15/15
  final pose within 0.80 m: 15/15
  JSON parse: 15/15
  fallback: 0/15
  max final error: 0.268 m
  ```
- 关键分组结果：
  - Trial 1-3，plant/potted plant：目标 `(-0.43, -2.92)`，最终误差 `0.268 m`。
  - Trial 4-6，chair：目标 `(-0.54, -0.69)`，最终误差 `0.196 m`。
  - Trial 7-15，purple boxes/shelf/ambiguous：目标 `(-6.78, 10.96)`，最终误差 `0.247 m`。
- 重要结论：
  - Node 6 的导航闭环已经跑通，Nav2 执行、目标坐标发布、最终位姿记录都正常。
  - 本次 `--timeout-sec 900` 是必要的，Trial 7 从当前位置去 shelf 区域耗时约 `735.713 s`，旧的 `240 s` 超时容易误判失败。
  - 视觉 grounding 仍需单独评估：部分结果里模型给出 `visible=False` 但仍返回坐标；Trial 13-15 的模糊指令全部落到 `shelf with purple boxes`，导航成功不代表语义目标一定正确。
- 临时日志位置：
  ```text
  /tmp/node6_static_tf.log
  /tmp/node6_pointcloud_to_scan.log
  /tmp/node6_base_footprint_tf.log
  /tmp/node6_nav2.log
  /tmp/node6_vln_bridge_rerun.log
  /tmp/node6_auto_trials_rerun.log
  ```
